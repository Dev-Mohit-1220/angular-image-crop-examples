# Angular Image Crop Examples

### For create angular app with a module structure directly in a same directory (including specific angular version)
```npx @angular/cli@18 new angular-crop --no-standalone --directory=. ```

### Add angular material specific version(18)
```ng add @angular/material@18```